version: "3.8"

services:
  mongo:
    image: mongo:latest
    container_name: tripplanner-db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - tripplanner_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: tripplanner

volumes:
  tripplanner_data:
